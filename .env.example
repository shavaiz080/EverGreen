# Firebase Configuration
# Replace with your actual Firebase project values
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com/

# For local development, you can use a local credentials file
# For production, set this environment variable with the JSON content of your Firebase service account key
# FIREBASE_CREDENTIALS={"type": "service_account", "project_id": "your-project-id", ...}
